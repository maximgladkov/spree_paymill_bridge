<%= render partial: 'spree/shared/paymill_card_form', locals: {payment_method: payment_method, name: @order.bill_address.full_name, currency: @order.currency} %>

<%= hidden_field_tag '', (@order.total * 100).to_i, id: "amount-int-#{payment_method.id}" %>
